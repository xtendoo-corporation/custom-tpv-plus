# tpv-plus-hide-files

Modulo para Odoo 18 que hereda `account.report_invoice_document` y oculta en la impresion de factura:

- Fecha de factura (`invoice_date`).
- Identificador principal (`o.name`).

